import { AdoptionModel } from '../../pojos/adoption-model';

describe('AdoptionModel', () => {
  it('should create an instance', () => {
    expect(new AdoptionModel()).toBeTruthy();
  });
});
